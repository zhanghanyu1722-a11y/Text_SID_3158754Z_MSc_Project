# Large binary tensors (frozen embeddings & Semantic-ID pickles; ~980 MB total,
# individual files up to ~600 MB) are intentionally NOT tracked here:
# they exceed the Moodle 230 MB limit and GitHub's 100 MB-per-file limit.
# The full set is distributed as a GitHub Release archive — see artifacts/README.md.
*.pt
*.zip

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Runtime / logs produced inside the GRID checkout (never committed)
logs/
.pipeline_state/

# OS
.DS_Store
Thumbs.db
